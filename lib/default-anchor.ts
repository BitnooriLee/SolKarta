/**
 * Default map / analysis anchor when the user has not selected a location yet.
 * Must stay in sync with MapView initial center (Stockholm).
 */
export const DEFAULT_ANCHOR = { lat: 59.3293, lng: 18.0686 } as const;
